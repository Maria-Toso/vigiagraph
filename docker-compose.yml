services:
  api:
    build: .
    ports:
      - "8000:8000"
    environment:
      DATABASE_PATH: /app/data/vigiagraph.db
    volumes:
      - vigiagraph-data:/app/data
    restart: unless-stopped

volumes:
  vigiagraph-data:

